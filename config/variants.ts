const container = "mx-4 md:mx-8 lg:mx-auto max-w-4xl min-h-[inherit]"
const columns = "flex items-center flex-wrap gap-y-4 -mx-2"
const column = "flex-none w-full md:flex-1 px-2"
const grid = "grid sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4"

export const variants = { container, columns, column, grid } as const